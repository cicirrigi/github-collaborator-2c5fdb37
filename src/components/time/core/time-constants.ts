/**
 * 🕒 VANTAGE LANE — TimePicker Constants (Enterprise)
 * Zero logic. Only shared constants used everywhere.
 */

export const UK_TIMEZONE = 'Europe/London';

// Default interval (Wheely / Blacklane standard)
export const DEFAULT_INTERVAL_MINUTES = 15;

// Lead time (minimum advance booking time)
export const DEFAULT_LEAD_TIME_MINUTES = 120; // 2 hours

// Minimum and maximum allowed hours in a day
export const MIN_HOUR = 0;
export const MAX_HOUR = 23;

// Minimum and maximum allowed minutes
export const MIN_MINUTE = 0;
export const MAX_MINUTE = 59;

// Scroll offset for auto-scroll (in px)
export const SCROLL_OFFSET = 120;

// UI constants
export const TIME_PICKER_ITEM_HEIGHT = 48; // px
